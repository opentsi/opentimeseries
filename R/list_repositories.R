list_repositories() <- function(source = "opentsi", exclude_repos = NULL){
  # get repo list 

  # then exclude all not: all 2 digit country patten, with . after, then take all 
  
  # and if exclude not null, then attach these newly excluded repos

  # return final list
  
}
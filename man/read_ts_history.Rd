% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_ts_history.R
\name{read_ts_history}
\alias{read_ts_history}
\title{Read the Full Version History of a Single Time Series}
\usage{
read_ts_history(
  series,
  remote_archive = "opentsi/ch.kof.globalbaro",
  dataset = NULL,
  archive_path = NULL,
  from = NULL,
  to = Sys.Date(),
  lastn = 100,
  consolidate = TRUE,
  by = "month",
  cache = FALSE,
  cache_dir = "~/.cache/opentimeseries",
  update = FALSE
)
}
\arguments{
\item{series}{character scalar; the time series subkey to query
(e.g. \code{"coincident"}). Must be length 1.}

\item{remote_archive}{character scalar; the GitHub archive in
\code{"owner/repo"} format. Defaults to \code{"opentsi/ch.kof.globalbaro"}.
Ignored when \code{archive_path} is supplied.}

\item{dataset}{character scalar; the dataset identifier for a local archive
(e.g. \code{"ch.kof.globalbaro"}). Required when \code{archive_path} is
set.}

\item{archive_path}{character scalar or \code{NULL} (default). When
supplied, reads from the local archive at
\code{archive_path/dataset} instead of GitHub.}

\item{from}{\code{Date} or date-coercible string; earliest vintage date to
include. Defaults to \code{NULL} (no lower bound).}

\item{to}{\code{Date} or date-coercible string; latest vintage date to include.
Defaults to \code{Sys.Date()}.}

\item{lastn}{integer; number of vintages to return. Defaults to \code{20}.
Values above 20 require \code{cache = TRUE}.}

\item{consolidate}{logical; if \code{TRUE} (default) only one vintage per period
(as defined by \code{by}) is returned — the most recent commit in that period.
Set to \code{FALSE} to get one vintage per calendar day with no further
consolidation.}

\item{by}{character scalar; period to consolidate by when
\code{consolidate = TRUE}. One of \code{"month"} (default), \code{"quarter"}, \code{"year"},
or \code{"day"}.}

\item{cache}{logical; if \code{TRUE} the repository is cloned to \code{cache_dir}
and all reads are served from the local clone. Required for
\code{lastn > 20}. Defaults to \code{FALSE}.}

\item{cache_dir}{character scalar; parent directory for the local clone.
Defaults to \code{"~/.cache/opentimeseries"}.}

\item{update}{logical; if \code{TRUE} and a local clone exists, runs
\verb{git pull} before reading. Ignored when \code{cache = FALSE}. Defaults to
\code{FALSE}.}
}
\value{
A \code{data.table} with columns \code{id}, \code{vintage_date}, \code{date}, \code{value}.
\code{vintage_date} is the commit date of that version; \code{date} is the
observation date within the series. Commits where the series file does
not yet exist are silently skipped.
}
\description{
Fetches every available vintage of one time series from a GitHub-hosted
Open Time Series archive. Each vintage corresponds to one Git commit, so
the result contains one row per observation date per commit, making it
straightforward to compare how a series has been revised over time.
}
\details{
For small numbers of vintages (\code{lastn <= 10}) the function fetches directly
from the GitHub raw-content API. For 11-20 vintages a warning is issued
recommending the cache. Above 20 vintages \code{cache = TRUE} is required — the
function errors with a ready-to-copy call using the cache.
}
\examples{
\dontrun{
# Fetch the last 10 vintages directly (no clone needed)
dt <- read_ts_history("coincident", lastn = 10)

# Fetch full history from a local clone
dt <- read_ts_history(
  "coincident",
  lastn  = 200,
  cache  = TRUE,
  update = TRUE
)

# Restrict to a date window
dt <- read_ts_history(
  "coincident",
  from  = "2023-01-01",
  to    = "2024-01-01",
  lastn = 50,
  cache = TRUE
)

# Pivot to a revision triangle
triangle(dt)
}
}
